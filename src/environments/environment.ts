export const environment = {
  production: true,
  // Production API base - matches BUSINESS.siteUrl on the backend (src/config/business.config.js).
  // Change once the API's production domain is known.
  apiUrl: 'https://beautymedica.rs/api/v1',
};
